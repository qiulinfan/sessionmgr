# Session archive

Run `./scripts/export-codex-sessions` from the repository root to populate
`repositories/` with immutable Codex session snapshots.

Generated snapshots are ordinary Markdown files. Commit and merge them with
Git; no database or Session Manager-specific remote service is required.
