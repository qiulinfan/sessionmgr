# Session Manager v0.7.0-dev Devlog

> Status: Development
> Date: 2026-08-15

## Objective and scope

Reduce repository-only build and release complexity without changing session
discovery, archive formats, safety checks, CLI compatibility, or the
already-reviewed v0.6.0 tag. Contributors now bring a supported Go toolchain,
local plus CI Windows release builds share one implementation, and portable
Windows users receive runtime environment guidance in the GUI.

This version does not remove legacy archive readers, migrations,
`cleanup-internal`, CLI aliases, safety tests, or persisted-data compatibility.

## Work completed

### Contributor toolchain

- Removed the POSIX and Windows automatic Go downloaders, wrapper launchers,
  checksum manifest, `make bootstrap`, and `make clean-tools`.
- Made `go` from `PATH` the Makefile default while retaining explicit `GO=...`
  override support and the existing no-CGO cross-platform targets.
- Updated the local Windows release builder to resolve the installed `go`
  executable directly.

### Release pipeline

- Split source validation/tests and Windows asset construction into Ubuntu and
  Windows jobs.
- Made the Windows job invoke `scripts/build-windows-release.ps1`, removing the
  duplicated icon generation, cross-build, PE, version-resource, runtime-version,
  and checksum implementation from workflow YAML.
- Removed the standalone SHA-256 manifest from the local builder, workflow
  artifact, verification steps, release upload, and release notes. Releases now
  contain only the two Windows executables.
- Preserved the independent publish job, read-only build permissions, and
  tag-only release boundary.

### Repository scope

- Removed the repository-specific `export-codex-sessions` helper and tracked
  `sessions/README.md`; neither was part of the shipped application, README,
  tests, or CI.
- Advanced unstamped development builds to `0.7.0-dev` and updated current
  requirements and release-pipeline documentation.

### Windows onboarding

- Added a read-only Environment panel that checks Git availability and the
  resolved Codex and DeepSeek Harness session directories on first load.
- Added bilingual missing-environment guidance. Windows users without Git see
  the official WinGet command and Git for Windows installer link; the app makes
  clear that the downloaded EXE does not require Go or GNU Make.
- Kept installation user-controlled: Session Manager does not run WinGet,
  elevate privileges, install software, or modify `PATH`.

## Decisions and rationale

- **Install Go instead of maintaining Go:** released users consume a standalone
  executable, while source contributors already need Git and GNU Make. Requiring
  Go 1.24+ removes two platform-specific downloaders, two launchers, pinned
  archive hashes, locking/staging code, and a repository-local toolchain state
  machine.
- **One Windows release builder:** keeping one reviewed PowerShell contract is
  smaller and less drift-prone than maintaining equivalent Bash and PowerShell
  implementations. GitHub Actions uses a native Windows runner for that step.
- **Two release assets are enough:** the separate SHA-256 manifest did not add
  useful product value for this small unsigned utility, so release construction
  and presentation now keep only the AMD64 and ARM64 executables.
- **Guide instead of silently provisioning:** Git is an external runtime
  dependency and its installer can elevate or change system state. The GUI
  detects it and presents official options, then lets the user choose and
  restart the portable app.
- **Compatibility is intentionally retained:** legacy readers and cleanup code
  protect user archives and require a separately designed major-version
  migration before removal.
- **v0.6.0 remains immutable:** its annotated tag continues to point to commit
  `4515830`; this v0.7 development work is not added to or substituted for that
  release.

## Validation performed

| Command or check | Result |
| --- | --- |
| PowerShell parser for `scripts/build-windows-release.ps1` | Pass |
| `go mod tidy` followed by `git diff --exit-code -- go.mod go.sum` | Pass; module files unchanged |
| `go mod verify` | Pass; all modules verified |
| `go vet ./...` on Windows/amd64 with Go 1.26.5 | Pass |
| `go test ./internal/app -count=1` on Windows/amd64 | Pass |
| `go run ./cmd/sessionmgr version` | Pass; printed `sessionmgr 0.7.0-dev` |
| `make check` in Ubuntu 26.04 WSL with Go 1.26.5 | Pass; vet, full tests, race tests, and build completed |
| `make dist` in Ubuntu 26.04 WSL | Pass; all supported Darwin, Linux, and Windows targets built with no CGO |
| `go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12 .github/workflows/release.yml` | Pass |
| `scripts/build-windows-release.ps1 -Version 0.7.2`, invoked outside the repository root after removing the checksum path | Pass; amd64 and arm64 PE files, embedded icon/version resources, runtime version, and temporary-file cleanup verified; release definitions contain no checksum asset |
| Active-tree stale-reference scan and `git diff --check` | Pass |
| `node --check internal/ui/static/app.js` | Pass after the environment-panel change |
| `go test ./internal/ui -count=1` and `go vet ./internal/ui` on Windows/amd64 | Pass |
| Repeated `make check` in Ubuntu 26.04 WSL after the onboarding change | Pass; full tests, race tests, vet, and build completed |
| Repeated `make dist` in Ubuntu 26.04 WSL after the onboarding change | Pass; all six no-CGO platform targets built |
| Live Windows GUI browser check | Pass; English and Chinese environment states rendered without console errors; desktop and 390 px narrow layouts had no horizontal overflow |
| `go vet ./...`, `go test ./...`, and actionlint after simplifying release assets | Pass |

The first release-builder attempt could not reach `proxy.golang.org` while
resolving `go-winres`; it exited without leaving temporary resources. The same
command passed with `GOPROXY=https://goproxy.cn,direct`. No GitHub release tag
was created for this development version.

## Compatibility and migration

- Program version advances from `0.6.0` to `0.7.0-dev` for unstamped builds.
- Config schema, repository/session metadata, archive layout, renderer, export
  JSON, CLI flags, and archive bytes are unchanged.
- `GET /api/state` gains an additive `environment` object for the bundled GUI;
  it reports platform, Git availability, and resolved source paths without
  changing persistent data.
- Future GitHub Releases contain the two architecture-specific Windows
  executables without a standalone checksum manifest.
- Source contributors and local release builders must now install Go 1.24+ on
  `PATH`; existing ignored `.tools/` caches are no longer read or managed.
- The v0.6.0 release commit and tag remain unchanged.

## Known limitations and risks

- The refactored workflow cannot be exercised until a future reviewed release
  tag; local action lint and native-script validation are required meanwhile.
- GitHub's Windows runner is slower than the former Ubuntu cross-build step, in
  exchange for using the exact same release implementation as local Windows.
- Removing repository-managed Go makes offline first builds the contributor's
  responsibility.
- Environment checks report whether each expected `sessions/` directory exists;
  they do not install Git or validate that every native session is exportable.

## Next steps

1. Validate the final workflow on the next release candidate without moving an
   existing tag.
2. Consider a separately scoped v1 migration before removing legacy layouts,
   CLI aliases, or `cleanup-internal`.

## Relevant files

- `Makefile`: installed-Go build, test, cross-check, and dist targets.
- `scripts/build-windows-release.ps1`: single local/CI Windows release builder.
- `.github/workflows/release.yml`: Ubuntu validation, Windows build, and guarded
  publish jobs.
- `internal/ui/server.go`: read-only runtime and source-directory checks.
- `internal/ui/static/`: bilingual environment setup panel.
- `README.md`: Windows runtime setup and contributor prerequisites.
- `docs/SPEC.md`: current build and release contract.

## Addenda

None.
